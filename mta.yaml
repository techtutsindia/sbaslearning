_schema-version: "3.2"
ID: Storage
version: 0.0.1
modules:
- name: Storage-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Storage-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comlearnelectronics.zip
      name: comlearnelectronics
      target-path: resources/
- name: comlearnelectronics
  type: html5
  path: electronics
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Storage-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Storage-html5-srv
    service-plan: app-host
- name: Storage-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Storage-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
